<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <IsPackable>false</IsPackable>
        <!-- Deliberate consumer of the experimental result-adapter surface under test. -->
        <NoWarn>$(NoWarn);ERGOEXP001</NoWarn>
        <!-- And of the experimental streaming surface, which these fixtures exist to exercise. -->
        <NoWarn>$(NoWarn);ERGOEXP003</NoWarn>
        <!-- ERGO007: a few probe handlers are delivered through engine-level dispatch or
             runtime registration, sites the closed-world analysis cannot see; the handlers
             do execute. Generator diagnostics ignore #pragma, so the suppression lives
             here — the rationale is commented at each declaration site. -->
        <NoWarn>$(NoWarn);ERGO007</NoWarn>
        <!-- ERGO013: two fixtures configure a default result adapter, and a compilation gets
             one of those (ERGO020) — so the whole assembly is judged against it, including
             the scores of fixtures that return a plain value and mean to throw. The rule is
             right for an application, where a configured default is a contract; here it would
             only assert that a test assembly is not one. ERGO013 itself is covered in
             Stella.Ergosfare.SourceGenerator.Test, over compilations written for it. -->
        <NoWarn>$(NoWarn);ERGO013</NoWarn>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="coverlet.collector" Version="6.0.4">
          <PrivateAssets>all</PrivateAssets>
          <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
        </PackageReference>
        <PackageReference Include="coverlet.msbuild" Version="6.0.4">
          <PrivateAssets>all</PrivateAssets>
          <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
        </PackageReference>
        <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.12.0"/>
        <PackageReference Include="xunit" Version="2.9.2"/>
        <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2"/>
    </ItemGroup>

    <ItemGroup>
        <Using Include="Xunit"/>
    </ItemGroup>

    <ItemGroup>
      <ProjectReference Include="..\..\src\Stella.Ergosfare.Commands.Extensions.MicrosoftDependencyInjection\Stella.Ergosfare.Commands.Extensions.MicrosoftDependencyInjection.csproj" />
      <ProjectReference Include="..\..\src\Stella.Ergosfare.Commands\Stella.Ergosfare.Commands.csproj" />
      <ProjectReference Include="..\..\src\Stella.Ergosfare.Core\Stella.Ergosfare.Core.csproj" />
      <ProjectReference Include="..\Stella.Ergosfare.Core.Extensions.MicrosoftDependencyInjection.Test\Stella.Ergosfare.Core.Extensions.MicrosoftDependencyInjection.Test.csproj" />
          <!-- The source generator as an analyzer: the frozen composition table is compiled,
           so a project whose messages must dispatch has to be generated over. -->
      <ProjectReference Include="..\..\src\Stella.Ergosfare.SourceGenerator\Stella.Ergosfare.SourceGenerator.csproj"
                        OutputItemType="Analyzer"
                        ReferenceOutputAssembly="false" />
</ItemGroup>

</Project>
